<?xml version="1.0" encoding="UTF-8"?>
<!--
  Logback configuration for CLI mode (no Spring Boot).

  When Spring Boot is active, it reinitializes logback using
  logback-spring.xml and this file has no effect.

  CLI commands reserve stdout for JSON output. All diagnostic
  output goes to stderr at WARN level by default.
-->
<configuration>
    <statusListener class="ch.qos.logback.core.status.NopStatusListener"/>
    <appender name="STDERR" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.err</target>
        <encoder>
            <pattern>%d{HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    <root level="WARN">
        <appender-ref ref="STDERR"/>
    </root>
</configuration>
