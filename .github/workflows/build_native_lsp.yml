#
# Copyright (C) 2017-2025 Dominic Heutelbeck (dominic@heutelbeck.com)
#
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
name: Build Native Language Server

on:
  workflow_dispatch:
  workflow_run:
    workflows: ["Testing and Deployment"]
    types: [completed]
    branches: [master]
  push:
    tags:
      - 'v*'
  pull_request:
    paths:
      - 'pom.xml'
      - 'sapl-api/**'
      - 'sapl-language-server/**'
      - 'sapl-parser/**'
      - 'sapl-pdp/**'
      - 'sapl-test-parser/**'
      - '.github/workflows/build_native_lsp.yml'

concurrency:
  group: native-lsp-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-native:
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact-name: sapl-language-server-linux-amd64
            executable: sapl-language-server
            profile: native-linux
          - os: windows-latest
            artifact-name: sapl-language-server-windows-amd64
            executable: sapl-language-server.exe
            profile: native
          - os: macos-latest
            artifact-name: sapl-language-server-macos-arm64
            executable: sapl-language-server
            profile: native
      fail-fast: false

    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45

    steps:
      - name: Harden Runner
        if: matrix.os == 'ubuntu-latest'
        uses: step-security/harden-runner@c6295a65d1254861815972266d5933fd6e532bdf # v2.11.1
        with:
          egress-policy: audit

      - name: Check out
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Install Native Build Tools (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential musl-dev musl-tools
          # Build static zlib for musl (required for static native-image)
          cd /tmp
          curl -sL https://zlib.net/zlib-1.3.1.tar.gz | tar xz
          cd zlib-1.3.1
          CC=musl-gcc ./configure --static --prefix=/usr/local/musl
          make -j$(nproc)
          sudo make install
          sudo ln -sf /usr/local/musl/lib/libz.a /usr/lib/x86_64-linux-musl/

      - name: Setup Visual Studio Build Tools (Windows)
        if: matrix.os == 'windows-latest'
        uses: ilammy/msvc-dev-cmd@0b201ec74fa43914dc39ae48a89fd1d8cb592756 # v1.13.0

      - name: Set up GraalVM
        uses: graalvm/setup-graalvm@22cc13fe88ef133134b3798e128fb208df55e1f5 # v1.2.3
        with:
          java-version: '21'
          distribution: 'graalvm'
          github-token: ${{ secrets.GITHUB_TOKEN }}
          native-image-job-reports: 'true'

      - name: Cache Maven packages
        uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-native-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-native-
            ${{ runner.os }}-maven-

      - name: Build Dependencies
        run: mvn -B install -pl sapl-language-server -am -DskipTests

      - name: Build Native Image
        run: mvn -B package -pl sapl-language-server -P${{ matrix.profile }} -DskipTests

      - name: Upload Native Executable
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: ${{ matrix.artifact-name }}
          path: sapl-language-server/target/${{ matrix.executable }}
          if-no-files-found: error
          retention-days: 7

      - name: Clean up Local Repository before Caching
        shell: bash
        run: rm -rf ~/.m2/repository/io/sapl

  publish-snapshot:
    name: Publish Snapshot
    needs: build-native
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_run' || (github.ref == 'refs/heads/master' && github.event_name == 'workflow_dispatch')
    permissions:
      contents: write

    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@c6295a65d1254861815972266d5933fd6e532bdf # v2.11.1
        with:
          egress-policy: audit

      - name: Download Linux artifact
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          name: sapl-language-server-linux-amd64
          path: artifacts/linux

      - name: Download Windows artifact
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          name: sapl-language-server-windows-amd64
          path: artifacts/windows

      - name: Download macOS artifact
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          name: sapl-language-server-macos-arm64
          path: artifacts/macos

      - name: Prepare release assets
        run: |
          mkdir -p release
          cp artifacts/linux/sapl-language-server release/sapl-language-server-linux-amd64
          cp artifacts/windows/sapl-language-server.exe release/sapl-language-server-windows-amd64.exe
          cp artifacts/macos/sapl-language-server release/sapl-language-server-macos-arm64
          chmod +x release/sapl-language-server-linux-amd64
          chmod +x release/sapl-language-server-macos-arm64

      - name: Generate checksums
        run: |
          cd release
          sha256sum ./* > SHA256SUMS.txt

      - name: Update snapshot release
        uses: softprops/action-gh-release@c95fe1489396fe8a9eb87c0abf8aa5b2ef267fda # v2.2.1
        with:
          tag_name: snapshot
          name: Latest Snapshot
          prerelease: true
          body: |
            ## Latest Development Snapshot

            This is an automatically updated pre-release containing the latest native builds from the `master` branch.

            **Warning:** This is a development snapshot and may be unstable.

            ### Downloads
            - `sapl-language-server-linux-amd64` - Linux x86_64 (static musl, works on any distro including NixOS)
            - `sapl-language-server-windows-amd64.exe` - Windows x86_64
            - `sapl-language-server-macos-arm64` - macOS Apple Silicon (ARM64)

            ### Verification
            Verify downloads using `SHA256SUMS.txt`:
            ```bash
            sha256sum -c SHA256SUMS.txt
            ```

            Built from commit: ${{ github.sha }}
          files: release/*
          fail_on_unmatched_files: true

  publish-release:
    name: Publish Release
    needs: build-native
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@c6295a65d1254861815972266d5933fd6e532bdf # v2.11.1
        with:
          egress-policy: audit

      - name: Download Linux artifact
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          name: sapl-language-server-linux-amd64
          path: artifacts/linux

      - name: Download Windows artifact
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          name: sapl-language-server-windows-amd64
          path: artifacts/windows

      - name: Download macOS artifact
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          name: sapl-language-server-macos-arm64
          path: artifacts/macos

      - name: Prepare release assets
        run: |
          mkdir -p release
          cp artifacts/linux/sapl-language-server release/sapl-language-server-linux-amd64
          cp artifacts/windows/sapl-language-server.exe release/sapl-language-server-windows-amd64.exe
          cp artifacts/macos/sapl-language-server release/sapl-language-server-macos-arm64
          chmod +x release/sapl-language-server-linux-amd64
          chmod +x release/sapl-language-server-macos-arm64

      - name: Generate checksums
        run: |
          cd release
          sha256sum ./* > SHA256SUMS.txt

      - name: Create release
        uses: softprops/action-gh-release@c95fe1489396fe8a9eb87c0abf8aa5b2ef267fda # v2.2.1
        with:
          name: SAPL Language Server ${{ github.ref_name }}
          body: |
            ## SAPL Language Server ${{ github.ref_name }}

            Native executables for the SAPL Language Server.

            ### Downloads
            - `sapl-language-server-linux-amd64` - Linux x86_64 (static musl, works on any distro including NixOS)
            - `sapl-language-server-windows-amd64.exe` - Windows x86_64
            - `sapl-language-server-macos-arm64` - macOS Apple Silicon (ARM64)

            ### Verification
            Verify downloads using `SHA256SUMS.txt`:
            ```bash
            sha256sum -c SHA256SUMS.txt
            ```

            ### Usage
            ```bash
            # Make executable (Linux/macOS)
            chmod +x sapl-language-server-*

            # Run in stdio mode (default for IDE integration)
            ./sapl-language-server-linux-amd64
            ```
          files: release/*
          fail_on_unmatched_files: true
