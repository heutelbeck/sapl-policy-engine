#
# Copyright (C) 2017-2025 Dominic Heutelbeck (dominic@heutelbeck.com)
#
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
name: Build Native SAPL Node

on:
  workflow_call:
    inputs:
      include-macos:
        description: Build the macOS native image (slow, ~60 min on free runner)
        type: boolean
        default: false
  workflow_dispatch:

jobs:
  build-native:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact-name: sapl-node-linux-amd64
            executable: sapl-node
            profile: native-linux
          - os: ubuntu-24.04-arm
            artifact-name: sapl-node-linux-arm64
            executable: sapl-node
            profile: native-linux-arm
          - os: windows-latest
            artifact-name: sapl-node-windows-amd64
            executable: sapl-node.exe
            profile: native
          - os: macos-latest
            artifact-name: sapl-node-macos-arm64
            executable: sapl-node
            profile: native
      fail-fast: false

    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    if: matrix.os != 'macos-latest' || inputs.include-macos
    timeout-minutes: 120

    steps:
      - name: Harden Runner
        if: runner.os == 'Linux'
        uses: step-security/harden-runner@58077d3c7e43986b6b15fba718e8ea69e387dfcc # v2.15.1
        with:
          egress-policy: audit

      - name: Check out
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install Native Build Tools (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential musl-dev musl-tools
          # GraalVM hardcodes x86_64-linux-musl-gcc in HostedMuslLibC regardless of arch
          sudo ln -sf /usr/bin/musl-gcc /usr/local/bin/x86_64-linux-musl-gcc
          # Get musl-compiled static zlib from Alpine's package manager
          docker run --rm -v /tmp/alpine-zlib:/out alpine:latest sh -c 'apk add --no-cache zlib-static && cp /usr/lib/libz.a /out/'
          sudo cp /tmp/alpine-zlib/libz.a /usr/lib/$(uname -m)-linux-musl/

      - name: Setup Visual Studio Build Tools (Windows)
        if: matrix.os == 'windows-latest'
        uses: ilammy/msvc-dev-cmd@0b201ec74fa43914dc39ae48a89fd1d8cb592756 # v1.13.0

      - name: Set up GraalVM
        uses: graalvm/setup-graalvm@54b4f5a65c1a84b2fdfdc2078fe43df32819e4b1 # v1.4.5
        with:
          java-version: '25'
          distribution: 'graalvm'
          github-token: ${{ secrets.GITHUB_TOKEN }}
          native-image-job-reports: 'true'

      - name: Cache Maven packages
        uses: actions/cache@cdf6c1fa76f9f475f3d7449005a359c84ca0f306 # v5.0.3
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-native-node-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-native-node-
            ${{ runner.os }}-maven-native-
            ${{ runner.os }}-maven-

      - name: Build Dependencies
        shell: bash
        run: mvn -B install -pl sapl-node -am -Dmaven.test.skip=true

      - name: Build Native Image
        shell: bash
        run: mvn -B package -pl sapl-node -P${{ matrix.profile }} -Dmaven.test.skip=true

      - name: Upload Native Executable
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: ${{ matrix.artifact-name }}
          path: sapl-node/target/${{ matrix.executable }}
          if-no-files-found: error
          retention-days: 7

      - name: Clean up Local Repository before Caching
        shell: bash
        run: rm -rf ~/.m2/repository/io/sapl
