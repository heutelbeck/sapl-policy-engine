set "demo set"
first or abstain errors propagate


policy "Change Resource Example"
permit
    action.http.path == "/changedstring";
obligation
	{
		"type" 		: "logAccess",
		"message"	: ("User " + subject.name + " has accessed: " + action.http.path)
	}
transform "***" + resource + "***"

policy "Time based obligation and deny (1/3)"
permit
    action.http.path == "/enforcetilldeny"
        | action.http.path == "/enforcedropwhiledeny"
        | action.http.path == "/enforcerecoverableifdeny";
    time.secondOf(<time.now>) < 20;
obligation
	{
		"type" 		: "logAccess",
		"message"	: "Time < 20"
	}

policy "Time based obligation and deny (2/3)"
permit
    action.http.path == "/enforcetilldeny"
        | action.http.path == "/enforcedropwhiledeny"
        | action.http.path == "/enforcerecoverableifdeny";
    time.secondOf(<time.now>) < 40;
obligation
	{
		"type" 		: "logAccess",
		"message"	: "Time < 40"
	}

policy "Time based obligation and deny (3/3)"
deny
    action.http.path == "/enforcetilldeny"
        | action.http.path == "/enforcedropwhiledeny"
        | action.http.path == "/enforcerecoverableifdeny";
    time.secondOf(<time.now>) < 60;
obligation
	{
		"type" 		: "logAccess",
		"message"	: "DENY ! Time < 60"
	}

policy "Permit Numbers"
permit
    action.http.path == "/numbers";
obligation
	{
		"type"		: "sendEmail",
		"recipient"	: "Adam Admin <admin@example.com>",
		"subject"	: "Data was accessed.",
		"message"	: ("Administrator " + subject.name + " has accessed: " + action.http.path)
	}
advice
	{
		"type" 		: "logAccess",
		"message"	: ("User " + subject.name + " has accessed: " + action.http.path)
	}
