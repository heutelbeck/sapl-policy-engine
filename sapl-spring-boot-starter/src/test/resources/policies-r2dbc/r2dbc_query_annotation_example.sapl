set "query annotations, age > 18"
first or abstain errors propagate

policy "permit query method (1)"
permit
    action == "fetchingByQueryMethod";
    subject.age > 18;
obligation {
               "type": "r2dbcQueryRewriting",
               "conditions": [ "active = true" ],
			   "selection": {
						"type": "whitelist",
						"columns": ["firstname"]
				}
             }
