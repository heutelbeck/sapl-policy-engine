set "query_annotation_person_repository"
first-applicable

policy "permit query method (1)"
permit
where
    action == "includeActivePersons";
    subject.admin == true;
obligation {
               "type": "r2dbcQueryManipulation",
               "conditions": [ "active = true" ]
             }

